# 🔌 إعداد Port في Railway

## 📍 Railway يسأل عن Port

عندما يسألك Railway: **"Enter the port your app is listening on"**

### ✅ الإجابة:

**اكتب:** `3000`

أو اتركه فارغاً إذا كان Railway يكتشفه تلقائياً.

---

## 🔍 لماذا 3000؟

في Backend (`backend/src/main.ts`):
```typescript
const port = process.env.PORT || 3000;
```

- Backend يستخدم `PORT` من Environment Variables
- إذا لم يكن موجود، يستخدم `3000` كقيمة افتراضية
- **Railway عادة يحدد PORT تلقائياً** ويضعه في متغير البيئة

---

## ✅ الحل:

### الطريقة 1: اتركه فارغاً (الأفضل)
- Railway عادة يكتشف Port تلقائياً
- أو يحدد PORT في Environment Variables تلقائياً

### الطريقة 2: اكتب 3000
- إذا طلب منك إدخال Port صراحة
- اكتب: `3000`

### الطريقة 3: استخدم متغير البيئة
- Railway عادة يضيف `PORT` تلقائياً
- لا حاجة لإضافته يدوياً في Variables

---

## 📝 ملاحظة مهمة:

- ✅ **Railway يحدد PORT تلقائياً** في معظم الحالات
- ✅ Backend يقرأ PORT من `process.env.PORT`
- ✅ إذا لم يكن موجود، يستخدم 3000
- ✅ **لا حاجة لإضافة PORT في Variables** - Railway يضيفه تلقائياً

---

## 🚀 بعد إدخال Port:

1. اضغط **"Save"** أو **"Continue"**
2. Railway سيستمر في الإعداد
3. تابع باقي الخطوات (Database, Variables, etc.)

---

**باختصار: اكتب `3000` أو اتركه فارغاً - Railway سيتعامل معه! ✅**

