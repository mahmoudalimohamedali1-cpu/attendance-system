# ⚙️ إعدادات Railway Service - خطوة بخطوة

## 📍 أنت الآن في: Service Settings

بناءً على ما تراه، اتبع الخطوات التالية بالضبط:

---

## ✅ الخطوة 1: إضافة Root Directory

### 1.1 في قسم "Source":
1. ابحث عن **"Add Root Directory"**
2. اضغط على الحقل الفارغ بجانب **"Add Root Directory"**
3. اكتب: **`backend`**
4. اضغط **"Save"** أو خارج الحقل (سيحفظ تلقائياً)

---

## 🔨 الخطوة 2: إضافة Build Command

### 2.1 في قسم "Build":
1. ابحث عن **"Custom Build Command"**
2. فعّل الخيار (Toggle) بجانب **"Custom Build Command"**
3. في حقل **"Build Command"** الذي سيظهر:
   - اكتب بالضبط:
     ```
     npm install && npm run build && npx prisma generate
     ```
4. اضغط **"Save"** أو خارج الحقل

---

## 🚀 الخطوة 3: إضافة Start Command

### 3.1 في قسم "Deploy":
1. ابحث عن **"Custom Start Command"**
2. فعّل الخيار (Toggle) بجانب **"Custom Start Command"**
3. في حقل **"Start Command"** الذي سيظهر:
   - اكتب بالضبط:
     ```
     npm run start:prod
     ```
4. اضغط **"Save"** أو خارج الحقل

---

## 🗄️ الخطوة 4: إضافة Database (Service منفصل)

### 4.1 الخروج من Settings:
1. اضغط على **"X"** في أعلى الصفحة
2. أو اضغط على اسم المشروع في الأعلى للعودة للـ Dashboard

### 4.2 إضافة Database:
1. في صفحة المشروع الرئيسية، اضغط **"New"** أو **"+"**
2. اختر **"Database"**
3. اختر **"PostgreSQL"**
4. انتظر حتى يتم الإنشاء (1-2 دقيقة)

### 4.3 نسخ DATABASE_URL:
1. اضغط على **Database service** (الذي تم إنشاؤه)
2. اذهب إلى **"Variables"** tab
3. ستجد **`DATABASE_URL`**
4. **انسخ هذا الرابط بالكامل**

---

## 🔧 الخطوة 5: إضافة Environment Variables

### 5.1 العودة إلى Backend Service:
1. اضغط على **Backend Service** (ليس Database)
2. اذهب إلى **"Variables"** tab (في الأعلى)

### 5.2 إضافة DATABASE_URL:
1. اضغط **"New Variable"** أو **"+"**
2. في **"Name"**: اكتب `DATABASE_URL`
3. في **"Value"**: الصق الرابط الذي نسخته من Database
4. اضغط **"Add"** أو **"Save"**

### 5.3 إضافة NODE_ENV:
1. اضغط **"New Variable"** مرة أخرى
2. في **"Name"**: اكتب `NODE_ENV`
3. في **"Value"**: اكتب `production`
4. اضغط **"Add"** أو **"Save"**

---

## 🚀 الخطوة 6: Deploy

### 6.1 Railway ينشر تلقائياً:
- بعد إضافة Variables، Railway يبدأ النشر تلقائياً
- أو اذهب إلى **"Deployments"** tab واضغط **"Redeploy"**

### 6.2 مراقبة النشر:
1. اذهب إلى **"Deployments"** tab
2. ستشاهد:
   - **"Building..."** - بناء المشروع
   - **"Deploying..."** - نشر المشروع
   - **"Running"** ✅ - المشروع يعمل

### 6.3 الحصول على الرابط:
1. اذهب إلى **"Settings"** → **"Networking"**
2. في قسم **"Public Networking"**:
   - اضغط **"Generate Domain"** إذا لم يكن موجود
   - **انسخ الرابط** - هذا هو Backend URL!
   - مثال: `https://attendance-system-production.up.railway.app`

---

## 📋 ملخص سريع:

1. ✅ **Root Directory:** `backend`
2. ✅ **Build Command:** `npm install && npm run build && npx prisma generate`
3. ✅ **Start Command:** `npm run start:prod`
4. ✅ **Database:** New → Database → PostgreSQL → انسخ DATABASE_URL
5. ✅ **Variables:** أضف `DATABASE_URL` و `NODE_ENV`
6. ✅ **Deploy:** انتظر → انسخ Public Domain

---

## ⚠️ ملاحظات مهمة:

- ✅ **Root Directory** مهم جداً - بدونها Railway لن يعرف أين ملفات المشروع
- ✅ **Build Command** يجب أن يحتوي على `npx prisma generate` لتوليد Prisma Client
- ✅ **Start Command** يجب أن يكون `npm run start:prod` للإنتاج
- ✅ **DATABASE_URL** يجب نسخه من Database service (ليس من Backend)

---

## 🐛 إذا واجهت مشاكل:

### Build فشل:
1. افتح **"Deployments"** → **"Logs"**
2. اقرأ رسالة الخطأ
3. تأكد من:
   - Root Directory = `backend` ✅
   - Build Command صحيح ✅

### Database connection failed:
1. تأكد من `DATABASE_URL` موجود في Variables
2. تأكد من نسخ الرابط كاملاً من Database service
3. أعد نشر Backend

---

**اتبع الخطوات بالترتيب وكل شيء سيعمل! 🚀**

